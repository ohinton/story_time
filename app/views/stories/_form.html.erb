<%= render 'layouts/errors', :object => @story %>

<%= form_for @story do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title, :class => "form-control" %>
  <%= f.submit :class => "btn btn-success contrib-btn" %>
<% end %>
